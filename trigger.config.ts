
import { defineConfig } from '@trigger.dev/sdk'

export default defineConfig({
  project: 'proj_etxguqawnjcuivmivmdu',
  runtime: 'node',
  logLevel: 'log',
  maxDuration: 60,
  dirs: ['./trigger'],
})
